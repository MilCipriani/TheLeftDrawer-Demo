name: nas
services:

  nginx:
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "${NETWORK_IP}:80:80"
      - "${NETWORK_IP}:443:443"
    networks:
      - nas